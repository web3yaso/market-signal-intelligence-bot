# Evaluation

最终版本：`gpt-5.6-luna`，prompt **d11d66e0def27315**，2026-09-15。提示词保存在 [demo/final/prompt.txt](demo/final/prompt.txt)，运行文件保留真实 raw 输出、结构化候选、逐步事件、用量和配置摘要。

## 1. 原基准的最终回归

20 条人工构造并标注的群聊消息，覆盖明确供需、别名、单位缺失、转发、重复报价、上下文回复、历史引用、矛盾信息和闲聊。原始划分是 dev 13 条、holdout 7 条；后者已经查看过，**此处复测是回归检查，不是新的独立盲测**。没有使用本轮 holdout 结果调参。

| 指标 | Dev | Holdout regression |
| --- | --- | --- |
| 决策匹配 | 12/13 | 7/7 |
| 标注字段匹配 | 116/116 | 60/62 |
| 额外错误信号 | 1 | 0 |
| 信号 precision | 8/9 | 4/4 |
| 信号 recall | 8/8 | 4/4 |
| 逐步事件状态匹配 | 13/13 | 7/7 |
| 逐步聚合匹配 | 13/13 | 7/7 |

完整数据：[dev evaluation.json](demo/final/dev/evaluation.json)、[holdout evaluation.json](demo/final/holdout/evaluation.json)。

字段匹配评估归一化后的输出，不是未经处理的模型准确率。字段分母包含 null 和漏报；额外输出另计误报，因此 dev 的 116/116 **不代表全部提取正确**。场景通过只说明状态和聚合匹配，也不表示语义字段全对。

剩余错误：

- S5/m01：模型把范围外 Claude 账号输出为 supply，按评测规则计额外错误信号；事件引擎因资源范围不符而不纳入市场统计。
- S4/m02：买方需求正确，但目标消息引用缺失，两个引用字段计错；需求独立创建，未影响本场景聚合。

原基准保持最初输入与标签，包括早期混合语言的模拟群聊。新增展示场景另外遵守中文 WeChat / 英文 Telegram 的约定，没有回写历史输入来维持已有分数。

## 2. 扩展演示的最终状态核对

48 条消息不作为新的独立准确率基准。它们用于展示并回归开发时发现的错误，人工预期保存在 dataset/demo/expected_final*.json，不进入模型输入。

| 场景 | 消息数 | API/schema 失败 | 有效卖方报价 | 实际供应 / 需求 | 卖价中位数 |
| --- | --- | --- | --- | --- | --- |
| D1 中文 WeChat | 36 | 1 | 8 | 1,110,000 / 2,100,000 USD 面值 | 3.70 |
| D2 英文 Telegram | 12 | 0 | 5 | 730,000 / 550,000 USD 面值 | 3.65 |

- **库存修复已验证**：D1/d12 的 Bob 剩余量更新为 150000，保留原卖价。
- **确认改价修复已验证**：D2/e09 正确更新 Bella 卖价为 3.65，最终数量、报价数、价格中位数及范围均符合人工预期。
- **D1 仍有运行失败**：d22 模型请求超时或网络失败，Henry 的 250k 报价没有建事件；d31 的 190k 剩余量更新找不到原事件而被拒绝。因此供应比人工预期 1300000 少 190000，有效报价从预期 9 个变为 8 个。没有重试或用人工结果补齐。
- D1/d25 的未定改价正确标为 unresolved，不改变 Cara 的已确认报价。

逐项差异：[D1 sanity_check.json](demo/final/D1/sanity_check.json)、[D2 sanity_check.json](demo/final/D2/sanity_check.json)。这说明单次请求失败会影响后续依赖事件；生产化应考虑可恢复重放和失败队列，原型保留无自动重试的取舍。

## 3. 修复方式

1. 将可选 quantity/price 中“已知字段全部为 null”的无信息对象规范为 null。未知键、部分值、有效数字不会被丢弃；原始响应不变。
2. 提示词明确：卖方已确认 asking price 是精确报价，is_soft=false；买方偏好和还价才属于 soft。业务校验仍拒绝未确认或软性卖方改价。

没有添加按消息 ID 命中的特例、第二条规则提取路径，或把 gold 填入预测结果。

## 4. 工程验证与复现

在独立临时目录创建新 Python 3.14 虚拟环境，从 requirements.lock 安装依赖：

- 37/37 unittest 通过。
- 20 条基准数据的一致性检查通过。
- fixture 回放 5/5 场景、20/20 状态、20/20 聚合匹配；它不测 LLM。
- pip check 无损坏依赖；已有运行目录可独立重绘报告。
- HTML 证据链接和窗口结构检查通过；筛选与证据展开脚本此前已通过隔离 JavaScript 检查。
- 浏览器截图级视觉验收未完成，未将静态检查冒充视觉验证。

## 5. 最终运行开销

本轮 68 次模型请求（13 dev + 7 holdout regression + 36 D1 + 12 D2），67 次有 usage：输入 59465、输出 19247 tokens。一次失败请求没有服务端 usage，因此 token 合计只是已返回用量，不能断言覆盖全部账单。

客户端逐请求耗时累计约 286 秒，包含网络，不是整个开发用时；并行任务的累计耗时也不等于墙钟时间。单价未配置，费用未知。之前的开发迭代不在这组最终运行用量中。

总作业用时约 **4 小时**，包含 AI 辅助开发，由提交者确认。
