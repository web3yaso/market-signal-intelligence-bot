# Real API mode requires both values. Never commit a real key.
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_BASE_URL=https://api.openai.com/v1
# Optional cost per million tokens, in your chosen billing currency.
# Unset prices => estimated_cost is null, not zero.
INPUT_PRICE_PER_MILLION=
OUTPUT_PRICE_PER_MILLION=
