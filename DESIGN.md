# Market Signal Intelligence Bot

V1.3 Lite 四小时实施设计

提交状态：核心链路及真实 API 演示已实现；最新验证与剩余错误见 EVALUATION.md。总用时约 4 小时（提交者确认，包含 AI 辅助的设计、数据、代码、测试及文档）。

## 1 目标与范围

用 20 条 Telegram／微信群聊 mock 消息演示：发布报价 → 转发去重 → 回复更新 → 撤回 → 价格与数量趋势。聚合只支持一种 OpenAI 美元面值额度及固定条款。模型 API 与 Python 环境须已可用；示例数值是验收预期。

采用单一 LLM 提取路径，确定性代码负责校验、单位规范和事件处理。扣分式 quality score 后置，只展示缺失字段和失败原因。V1.2 留作设计参考，不再作为四小时待办清单。

## 2 接入与智能处理的边界

模拟 TG／微信数据源 → 接入层输出统一 Message（JSONL 回放）→ 智能处理层：去重 → 回复上下文 → LLM 提取 → 校验与归一化 → 事件更新 → 快照 → 报告。

接口边界是第 3 节的 Message；JSONL 只是本期的存储与回放方式。接入层负责平台字段、ID、时间及引用关系映射，不判断供需或报价。智能处理层接收 Message 和群业务配置，不依赖平台 SDK 或平台分支。未来连接器输出同一格式即可复用下游；本期用已规范化的 mock 消息替代真实连接器。

采用单 LLM，每条非重复消息最多一次提取，可输出多个 Observation。上下文仅取已处理的直接回复链，最多 3 条；指代不唯一则 unresolved。后续是否增加规则路径由调用成本决定。

LLM 只输出候选，校验失败不变更状态。输入上限 3000 tokens、输出 1000 tokens、超时 20 秒，不自动重试；超限标 unresolved，超时或格式错误标 failed。保存模型、prompt 标识和提取结果；不执行消息中的指令。报告 token、耗时及按配置单价估算的费用。

## 3 最小数据契约

| 对象 | 必要信息 |
| --- | --- |
| Message | channel_id、message_id、sender_id、timestamp、text、reply_to、forwarded_origin；引用使用 channel 与 message 复合键 |
| Observation | action、resource、quantity、price、target_message、assertion_mode、source_message_ids、evidence_quote、unresolved_reason |
| Event | ID、owner、origin_message、side、剩余量及单位、价格及口径、status、supporting_message_ids |
| Snapshot | step、as_of、事件状态副本、聚合值、样本 ID、排除计数 |

action 限定为供给、需求、设置剩余量、改价、撤回、忽略、未解析；assertion_mode 区分直接声明与转述。保留消息 ID 和原文摘录，不实现逐字段 span。未知值为 null，不能填零。

channel_id 在不同平台间唯一，timestamp 带时区；reply_to 和 forwarded_origin 均用复合引用。平台无法提供或确认的引用／转发来源填 null，不猜测关联。

## 4 业务报价口径

明确配置适用群聊、OpenAI 额度形态与条款：价格单位为 CNY / USD 面值额度。数值绑定原文，隐含单位绑定 convention_id。数量单位另行配置，不由价格分母自动推断。

“Still looking for 500k OpenAI, below 4 if possible.”表示求购，软性偏好 p < 4 CNY / USD 面值额度，不是卖方报价。500k 解析为 500000，但数量约定也明确时才计入需求量。以参考值 7 解释约 5.7 折即可，不实现实时汇率或折扣指标。

## 5 确定性事件规则

| 情况 | 处理 |
| --- | --- |
| 重复消息或明确转发 | 重复复合键不再处理；转发绑定原消息事件，只增加证据，不加数量 |
| 相同数量与报价 | 无明确来源关联则不合并；同一发布者也可能有不同批次 |
| 买方表达意向 | 记录需求或询问，不减少供应；明确关联的已有需求不重复创建 |
| 原发布者回复剩余量 | 唯一目标、单位一致时，用新剩余量替换原值 |
| 原发布者回复改价或撤回 | 更新唯一目标；状态仅 active / retracted；改价不改变数量 |
| 第三方转述或含糊指代 | 保留证据，不修改事件；目标不明则 unresolved |

不做成交扣减、补货增量、预留或 TTL。复杂交易保留未支持原因，不猜库存。重复消息不得重复改变状态；同键不同内容标为不支持编辑。撤回后重新发布视为新事件。

## 6 聚合与历史

mock 群聊按设定时间和稳定复合键回放。每处理完一条消息便复制事件状态并保存快照，as_of 为该步处理后的模拟时间，同时间戳用 step 区分。后续更新不能改写旧快照；乱序、编辑和追溯修正后置。

只算四项：卖方报价中位数、已观察供应量、已观察需求量、去重活跃事件数。价格仅使用同资源、条款、币种和分母的单点卖方报价，排除预算上限。数量仅累加明确且可比的单位。空价格为 null，未知数量排除并计数。

没有 TTL，active 仅表示尚未明确撤回，不保证仍有货。输出一张模拟时间趋势图和一张可查看输入消息的事件表。图标注“模拟数据中的未撤回供应”及报价样本数，展示系统随消息更新的聚合能力，不据此推导真实市场走势。

## 7 完整演示与预期结果

mock 演示群明确约定数量为 USD 面值额度，价格为 CNY / USD 面值额度，资源及条款一致。数值是处理完该行消息后的状态。

| 步骤与消息 | 供应量 | 卖方中位价 |
| --- | --- | --- |
| 1 Alice：有 100k OpenAI 额度，3.8 出 | 100000 | 3.8 |
| 2 明确转发 Alice 的原消息 | 100000 | 3.8 |
| 3 Bob：独立发布相同数量与报价 | 200000 | 3.8 |
| 4 Dan：求 500k OpenAI，below 4 if possible | 200000 | 3.8 |
| 5 Alice 回复自己的 offer：还剩 60k | 160000 | 3.8 |
| 6 Alice 回复同一 offer：改成 3.6 | 160000 | 3.7 |
| 7 第三方回复 Alice：听说没货了 | 160000 | 3.7 |
| 8 Alice 回复同一 offer：撤回 | 100000 | 3.8 |

步骤 4 后需求为 500000，未收到 Dan 撤回则保持不变。步骤 8 后读取步骤 6 快照，仍应得到供应 160000、中位价 3.7。以上保留为验收预期；最终版本的实际结果见 EVALUATION.md。

## 8 最小评测

20 条 mock 消息组成 5 个场景，覆盖明确供需、回复与转发、重复报价、矛盾与历史信息、未知单位和无关内容。开发集 13 条，留出集 7 条、2 个完整场景；最初按此划分开展验证。该留出集如今已查看，最终复测作为回归，不称新的盲测；不根据其结果调整 prompt，同一模拟事件及其改写不跨集合。标签以最终输入为准，见第 11 节。

人工标注 action、资源、价格、数量和每个场景最终事件及聚合结果。重点检查以下不变量：

- 转发不加库存；无关联来源的相同报价不合并。
- 买方意向不扣供应；预算上限不进入卖方中位价。
- 唯一目标和原发布者的明确回复才能修改事件。
- 剩余量替换正确；重复处理不再次改变状态。
- 未知单位排除对应数量；后续撤回不改变旧快照。

报告场景通过数、字段正确数/标注数、unresolved/failed 数和调用开销；额外输出的错误信号也计错，不把拒答从召回统计中删除。mock 输入配合真实 API 可评测提取；若模型输出也用固定结果替代，只验证下游，不计为真实 LLM 评测。

## 9 时间预算与交付

0–30 分钟：核对现有群聊样例、标签和输入格式；30–90：提取与校验；90–150：事件与快照；150–180：报告；180–210：评测与修复；210–240：README 和缓冲。四小时到点记录未完成项。

交付可运行入口、样例与标签、demo 输出、简短 README。代码可压为 schema.py、extract.py、events.py、report.py、evaluate.py 五个文件，JSONL 输入输出即可。记录实际耗时以及实现、mock、未支持部分。

后置 quality score、双路径、模型对照、复杂生命周期、向量去重、真实平台接入、数据库服务和精致 UI。时间不足先减少表达覆盖与交互，保留完整链路与关键检查。

## 10 与原题评分标准的对照

依据作业第 4 页 How we evaluate the submission。以下为设计覆盖与提交证据，不代表实现已通过，也不是预估得分。

| 维度与权重 | 设计覆盖及提交时的证明 |
| --- | --- |
| 问题定义 20% | 第 1、3、4 节：原子信号与事件分开，明确单位、业务假设和未知值；提交 schema 与两条含糊样例 |
| AI 提取设计 20% | 第 2 节：有界上下文、结构化候选、预算和失败处理；展示真实模型调用的提取及拒答记录 |
| 系统工程 20% | 第 2、3、5、6 节：平台接入与智能处理以 Message 分界；证明两类输入共用下游、重复回放幂等及历史快照不变 |
| 数据推理 15% | 第 4–7 节：单位归一、明确来源去重、预算与报价分开；中位数按事件等权抗极端值，不消除抽样偏差 |
| 评测 15% | 第 8 节：完整场景标注、留出案例、字段与聚合检查；提交可复现结果及失败分析 |
| 沟通与取舍 10% | 第 9 节：单一路径、简单展示、明确后置；README 写清运行方式、mock、实际耗时及限制 |

## 11 模拟群聊样例与验收

项目数据是自行构造的 Telegram／微信群聊消息。dataset 已提供 20 条消息、21 条观测标签、20 个逐步预期快照，分为 5 个场景。自然讨论可以启发表达，但无需准备论坛来源 ID、改编记录或来源追溯表；README 标明 mock 数据及场景假设即可。

构造时保留口语、简称、询价和无关插话，设置群 ID、假名、模拟时间、引用回复和转发关系。需要依赖上下文的案例提供可见的回复链，同时保留故意缺少上下文的案例，以检验是否正确返回 unresolved。

现有 S3 场景通过两层回复表达供给撤回：

- m01 nora：出 OpenAI 官方 API 额度，30000 美元面值，每美元面值 3.2 人民币，预付。
- m02 owen 引用 m01：还有吗？reply_to 指向 m01。
- m03 nora 引用 m02：没了。reply_to 指向 m02。

m03 的上下文包含 m02、m01，由原发布者撤回唯一供给事件；撤回后供应量为 0、卖方中位价为 null。聊天里的 forwarded_origin 用于关联转发原消息及去重，是业务输入字段；普通复制粘贴没有明确来源时不能当成已确认转发。

| 准备事项 | 最小处理方式 |
| --- | --- |
| 群聊时间 | 直接设定用于回放的时间线；图表与 README 标明 mock 数据 |
| 标签复核 | 以最终聊天输入为准核对 action、资源、价格、数量、目标事件及聚合预期 |
| 业务语义 | “I would like to order some” 是意向；“日万刀起”不是库存；按消耗结算与预付额度不能混算 |
| 留出与难例 | 同一模拟事件及改写放在同一集合。保留单位未知、第三方转述和噪声，冻结标签后再调 prompt |
| 接口验收 | TG／微信场景均由相同回放入口产出 Message，调用同一下游；无需平台特定的提取或事件规则 |

gold、预期快照及标签理由不进入模型输入。对歧义字段写清允许答案或 null 的理由，单独报告结果。真实 API 的评测结论仅限这些 mock 场景。新增展示数据以中文 WeChat、英文 Telegram 分群；早期基准保持原始内容，不追改历史输入。dataset/validate.py 仅检查数据一致性；独立测试及真实模型结果另见 EVALUATION.md。

对应评分：第 2、3 节落实 Suggested interface boundary，以统一消息格式分离接入和智能处理。用供需、去重、撤回及快照验证数据推理与工程能力；用留出场景、逐步预期和失败记录说明评测。README 解释 mock 接入、单一路径、实际耗时及后置项。

讨论准备：用第 7 节的转发与第三方撤回解释取舍。没有 reply 时允许未解析；没有 TTL 会保留过时报价。若消息量增至 100 倍，先测调用成本与失败率，再考虑缓存、有界并发和增量聚合；不在本原型实现。展示 1–2 个实际运行中的失败案例及下一步改进。
